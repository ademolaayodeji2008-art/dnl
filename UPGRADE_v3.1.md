# v3.1 Notifications & Email

Built directly from the user-provided latest working baseline.

Adds unread notification bell, notification drawer, mark read/all read, direct PV opening, workflow-targeted notifications and professional email delivery. SMTP secrets remain in .env.

Run: npm install, npx prisma format, npx prisma generate, npx prisma migrate deploy, npm start. No new migration is expected.
